{% extends "PublicationBundle:layout.html.twig" %}

{% block publication_body %}
    <h1>{{ sujet.titre }}</h1>
    <hr/>
    <div class="col-md-8">{{ sujet.contenu | raw }}</div>
    <div class="col-md-12">
        <a href="{{ path("publication_edit", {"id" : sujet.id}) }}" class="btn btn-warning">Modifier</a>
    </div>
{% endblock %}